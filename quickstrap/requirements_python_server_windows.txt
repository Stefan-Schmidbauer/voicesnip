# VoiceSnip Python Requirements - Server Profile (Windows)
# Deepgram (cloud) + Faster Whisper Server via network/remote

numpy>=1.20.0
sounddevice>=0.4.6
pynput>=1.7.6
requests>=2.31.0
python-dotenv>=1.0.0
pillow>=10.0.0

# Whisper dependencies for local CPU transcription
faster-whisper>=1.0.0

# NOTE: This profile connects to a remote Faster Whisper Server
# No GPU packages needed locally (server handles GPU acceleration)
