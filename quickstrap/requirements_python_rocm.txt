# Ancroo Voice Python Requirements - ROCm Profile
# Full-featured: Deepgram (cloud) + Whisper (local CPU + AMD GPU via ROCm)

# Base requirements
numpy>=1.20.0
sounddevice>=0.4.6
pynput>=1.7.6
requests>=2.31.0
python-dotenv>=1.0.0
pillow>=10.0.0
customtkinter>=5.2.0

# Whisper CPU (faster-whisper / CTranslate2)
faster-whisper>=1.0.0

# Whisper ROCm GPU (transformers + PyTorch ROCm)
# NOTE: PyTorch ROCm is installed via post-install script from:
# https://download.pytorch.org/whl/rocm6.2
transformers>=4.36.0
accelerate>=0.26.0
