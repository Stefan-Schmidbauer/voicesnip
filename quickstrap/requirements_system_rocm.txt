# Ancroo Voice System Requirements - ROCm Profile
# For Debian/Ubuntu systems (apt packages)

# Include all basis requirements
python3
python3-pip
python3-venv
python3-dev
python3-tk
libportaudio2
portaudio19-dev
xdotool
git
pkg-config

# ROCm GPU drivers
# NOTE: PyTorch ROCm is installed via pip post-install script
# User must have ROCm drivers installed (verify with rocm-smi)
# Install ROCm: https://rocm.docs.amd.com/projects/install-on-linux/en/latest/
