# VoiceSnip System Requirements - CPU Profile
# For Debian/Ubuntu systems (apt packages)
#
# CPU-only profile: no GPU drivers or GPU runtime libraries required.
# Whisper runs on the CPU via faster-whisper (CTranslate2, int8).

# Core system packages
python3
python3-pip
python3-venv
python3-dev
python3-tk
libportaudio2
portaudio19-dev

# Text insertion (display server detected at runtime, both stacks installed)
# X11:
xdotool
# Wayland (uinput-based typing + clipboard paste):
ydotool
ydotoold
wl-clipboard

git
pkg-config
