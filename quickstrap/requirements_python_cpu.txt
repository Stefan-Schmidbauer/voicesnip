# VoiceSnip Python Requirements - CPU Profile
# Whisper local on CPU (no GPU required)

# Base requirements
numpy>=1.20.0
sounddevice>=0.4.6
pynput>=1.7.6
evdev>=1.6.0
requests>=2.31.0
python-dotenv>=1.0.0
pillow>=10.0.0
customtkinter>=5.2.0

# Whisper CPU (faster-whisper / CTranslate2, runs int8 on CPU)
faster-whisper>=1.0.0
