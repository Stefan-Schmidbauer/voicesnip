# VoiceSnip Python Requirements - CUDA Profile (Windows)
# Full-featured: Deepgram (cloud) + Whisper (local CPU/GPU)

# Base requirements
numpy>=1.20.0
sounddevice>=0.4.6
pynput>=1.7.6
requests>=2.31.0
python-dotenv>=1.0.0
pillow>=10.0.0
customtkinter>=5.2.0

# Whisper dependencies for local transcription
faster-whisper>=1.0.0

# GPU acceleration (NVIDIA CUDA 12.x required)
nvidia-cudnn-cu12>=9.0.0
nvidia-cublas-cu12>=12.0.0
